# ~~ Generated by projen. To modify, edit .projenrc.ts and run "bun run projen".
name: Build .contextbridge image

on:
  push:
    branches: [main]
    paths:
      - '.contextbridge/**'
      - '!.contextbridge/docker-compose.yml'
  workflow_dispatch:

jobs:
  push:
    permissions:
      contents: read
      id-token: write
    uses: contextbridge/contextbot-actions/.github/workflows/push-image.yml@v1
