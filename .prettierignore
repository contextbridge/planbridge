dist/
node_modules/
coverage/
tools/
.claude/
bun.lock
packages/*/.astro/
packages/website/src/content/docs/cli/

# Drizzle-generated migration artifacts. snapshot.json formatting is dialect-
# dependent and changes shape between drizzle-kit versions; don't fight it.
packages/storage/generated/

# Prettier treats .hbs as HTML and collapses the markdown these templates emit.
*.hbs

# release-please owns formatting of files it writes; don't fight it.
CHANGELOG.md
harnessIntegrations/claude/.claude-plugin/plugin.json

# ContextBot owns formatting of agents and its own rules
AGENTS.md
.contextbridge/rules
